class Renach{
    pontos: string;
    validade: string;
    situacao: string;

    constructor(){
        this.pontos = '';
        this.validade = '';
        this.situacao = '';
    }

    getpontos(){
        //const response = this.repositoryRenach.findOne(user)
        //return response
    }
    getvalidade(){
        //const response = this.repositoryRenach.findOne(user)
        //return response
    }
    getsituacao(){
        //const response = this.repositoryRenach.findOne(user)
        //return response
    }

}